services:
  - type: web
    name: followuptpm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: followuptpm-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

databases:
  - name: followuptpm-db
    plan: free
